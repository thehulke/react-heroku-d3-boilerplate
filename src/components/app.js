import React from 'react';
import Chart from './chart'

let App = React.createClass({
    render: function() {
        return (
            <div>
                <Chart/>
            </div>
        );
    }
});

export default App;
